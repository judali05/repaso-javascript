var edad = parseInt(prompt("Ingresa tu edad solo numeros:"));

    // Verificar si la edad es mayor o menor que 18 con if
if (edad >= 18) {
    document.write("Eres mayor de edad, tienes " + edad );

}else {
    document.write("Eres menor de edad, tienes " + edad);

}